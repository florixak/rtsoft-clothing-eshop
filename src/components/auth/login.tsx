import { LoginForm } from "../form/login-form";

const Login = () => {
  return (
    <section className="container grid items-center gap-6 pt-6 pb-8 md:py-10 mx-auto max-w-md">
      <LoginForm />
    </section>
  );
};

export default Login;
